name: plugin_crypto
description: "Flutter FFI plugin wrapping OpenSSL for crypto operations on Linux, Android, and iOS."
version: 0.0.1
homepage: https://github.com/medalha01/Plugin-Crypto
repository: https://github.com/medalha01/Plugin-Crypto
issue_tracker: https://github.com/medalha01/Plugin-Crypto/issues
topics:
  - cryptography
  - openssl
  - ffi
  - post-quantum
  - flutter

environment:
  sdk: ^3.11.5
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2
  ffi: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  ffigen: ^20.1.1
  coverage: ^1.14.0
  glados: ^1.1.7

false_secrets:
  - test/fixtures/certificates.dart
  - test/zone11_cms_encrypt_decrypt_test.dart
  - test/zone28_cades_test.dart
  - test/**/*.dart

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' specifies the class (in Java, Kotlin, Swift, Objective-C, etc.)
  # which should be registered in the plugin registry. This is required for
  # using method channels.
  # The Android 'package' specifies package in which the registered class is.
  # This is required for using method channels on Android.
  plugin:
    platforms:
      android:
        ffiPlugin: true
        package: com.tcc.plugin_crypto
        pluginClass: PluginCryptoPlugin
      ios:
        ffiPlugin: true
        pluginClass: PluginCryptoPlugin
      linux:
        ffiPlugin: true
        pluginClass: PluginCryptoPlugin

  # To add assets to your plugin package, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  #
  # For details regarding assets in packages, see
  # https://flutter.dev/to/asset-from-package
  #
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # To add custom fonts to your plugin package, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts in packages, see
  # https://flutter.dev/to/font-from-package
